<% flash.each do |message_type, message| %>
  <div class="<%= flash_type_class(message_type) %> items-center text-white p-2 text-center w-full text-sm md:text-base">
    <%= message %>
  </div>
<% end %>

