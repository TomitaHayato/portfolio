<div class="m-1 sm:m-3" id="reward-zone-<%= reward.id %>">
  <% if reward.users.include?(current_user) %>
    <%= render 'rewards/reward_card', reward: reward %>
  <% else %>
    <%= render 'rewards/description', reward: reward %>
  <% end %>
</div>